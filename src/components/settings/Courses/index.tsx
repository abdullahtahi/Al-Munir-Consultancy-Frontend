import Grid from '@mui/material/Grid2';
import CoursesList from 'src/components/operations/Courses/CoursesList';

const CoursesIn = () => {
  return (
    <Grid container spacing={3}>
      <CoursesList />
    </Grid>
  );
};

export default CoursesIn;
